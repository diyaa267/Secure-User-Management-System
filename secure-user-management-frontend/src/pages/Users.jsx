import {useEffect,useState} from "react";
import {Link} from "react-router-dom";
import API from "../services/api";
import "../styles/admin.css";
import "../styles/users.css";
export default function Users(){const[u,setU]=useState([]);const[m,setM]=useState("");const load=()=>API.get("/users").then(r=>setU(r.data.users)).catch(e=>setM(e.response?.data?.error||"Failed to load users"));useEffect(()=>{load()},[]);const del=async id=>{if(!window.confirm("Delete this user?"))return;try{await API.delete(`/users/${id}`);load()}catch(e){setM(e.response?.data?.error||"Delete failed")}};return <div className="admin-page"><div className="admin-wrap"><header className="admin-head"><div><p>USER MANAGEMENT</p><h1>Registered Users</h1><p>Review accounts and manage access from one place.</p></div><div className="admin-actions"><Link to="/admin">Admin</Link><Link to="/dashboard">Dashboard</Link></div></header><section className="admin-card users-card">{m&&<div className="users-error">{m}</div>}<div className="users-table-wrap"><table><thead><tr><th>ID</th><th>Name</th><th>Email</th><th>Role</th><th>Action</th></tr></thead><tbody>{u.map(x=><tr key={x.id}><td>#{x.id}</td><td>{x.fullname}</td><td>{x.email}</td><td><span className={`user-role ${x.role}`}>{x.role}</span></td><td><button onClick={()=>del(x.id)}>Delete</button></td></tr>)}</tbody></table></div></section></div></div>}
